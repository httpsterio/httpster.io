<section class="top-nav">
    {% for item in navigation.main %}
      <a class="rounded-btn" href="{{ item.url }}"{{ helpers.getLinkActiveState(item.url,page.url)|safe}}>{{ item.text }}</a>
    {% endfor %}
</section>